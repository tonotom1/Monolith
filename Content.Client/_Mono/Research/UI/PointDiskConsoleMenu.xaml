<controls:FancyWindow xmlns="https://spacestation14.io"
                     xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
                     xmlns:graphics="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
                     Title="{Loc 'point-disk-ui-name'}"
                     MinSize="400 260"
                     SetSize="400 260">
    <BoxContainer Orientation="Vertical"
                  HorizontalExpand="True"
                  VerticalExpand="True"
                  Margin="10 10 10 10">
        <Button Name="ServerButton"
                Text="{Loc 'lathe-menu-server-list'}"
                HorizontalAlignment="Left"></Button>
        <PanelContainer Margin="0 10 0 0" VerticalExpand="True">
            <PanelContainer.PanelOverride>
                <graphics:StyleBoxFlat BackgroundColor="#000000FF"/>
            </PanelContainer.PanelOverride>
            <BoxContainer Orientation="Vertical" VerticalExpand="True" HorizontalExpand="True" VerticalAlignment="Center">
                <Label Name="TotalLabel"
                       HorizontalAlignment="Center">
                </Label>
                <BoxContainer MinHeight="20"></BoxContainer>
                <Button
                    Name="Print1KButton"
                    Text="{Loc 'point-disk-ui-print-1k-button'}"
                    MaxWidth="240"
                    HorizontalAlignment="Center"
                    VerticalExpand="False">
                </Button>
                <Button
                    Name="Print5KButton"
                    Text="{Loc 'point-disk-ui-print-5k-button'}"
                    MaxWidth="240"
                    HorizontalAlignment="Center"
                    VerticalExpand="False">
                </Button>
                <Button
                    Name="Print10KButton"
                    Text="{Loc 'point-disk-ui-print-10k-button'}"
                    MaxWidth="240"
                    HorizontalAlignment="Center"
                    VerticalExpand="False">
                </Button>
            </BoxContainer>
        </PanelContainer>
    </BoxContainer>
</controls:FancyWindow>
